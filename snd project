import { useAuth } from "@/contexts/AuthContext";
import { Button } from "@/components/ui/button";
import { Link } from "react-router-dom";
import { BookOpen, MessageCircleQuestion, Users, ArrowRight } from "lucide-react";

const Index = () => {
  const { user } = useAuth();

  return (
    <div className="min-h-screen bg-background">
      {/* Nav */}
      <nav className="border-b bg-card/80 backdrop-blur-sm sticky top-0 z-50">
        <div className="container mx-auto flex items-center justify-between h-16 px-4">
          <Link to="/" className="font-serif text-xl text-foreground">📚 StudyHub</Link>
          <div className="flex gap-3">
            {user ? (
              <Button asChild><Link to="/dashboard">Dashboard</Link></Button>
            ) : (
              <>
                <Button variant="ghost" asChild><Link to="/login">Login</Link></Button>
                <Button asChild><Link to="/register">Sign Up</Link></Button>
              </>
            )}
          </div>
        </div>
      </nav>

      {/* Hero */}
      <section className="container mx-auto px-4 py-24 text-center">
        <h1 className="text-5xl md:text-6xl font-serif text-foreground mb-6 animate-fade-in">
          Share Notes. Solve Doubts.<br />Learn Together.
        </h1>
        <p className="text-lg text-muted-foreground max-w-2xl mx-auto mb-10 animate-fade-in" style={{ animationDelay: "0.1s" }}>
          A collaborative platform where students share study materials, ask questions, and help each other succeed academically.
        </p>
        <div className="flex gap-4 justify-center animate-fade-in" style={{ animationDelay: "0.2s" }}>
          <Button size="lg" asChild>
            <Link to={user ? "/dashboard" : "/register"}>
              Get Started <ArrowRight className="ml-2 h-4 w-4" />
            </Link>
          </Button>
        </div>
      </section>

      {/* Features */}
      <section className="container mx-auto px-4 pb-24">
        <div className="grid md:grid-cols-3 gap-8">
          {[
            { icon: BookOpen, title: "Share Notes", desc: "Upload and download study notes in PDF, DOC, and more. Organized by subject for easy access." },
            { icon: MessageCircleQuestion, title: "Ask Doubts", desc: "Post your questions and get answers from fellow students. No doubt is too small." },
            { icon: Users, title: "Community Answers", desc: "Help others by answering their doubts. Upvote the best answers to highlight quality." },
          ].map((f, i) => (
            <div
              key={f.title}
              className="bg-card border rounded-lg p-8 text-center animate-fade-in"
              style={{ animationDelay: `${0.3 + i * 0.1}s` }}
            >
              <div className="inline-flex items-center justify-center w-14 h-14 rounded-full bg-primary/10 text-primary mb-5">
                <f.icon className="h-7 w-7" />
              </div>
              <h3 className="font-serif text-xl mb-3 text-foreground">{f.title}</h3>
              <p className="text-muted-foreground text-sm leading-relaxed">{f.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t py-8 text-center text-muted-foreground text-sm">
        <p>© 2026 StudyHub — Students Notes & Doubt Sharing Platform</p>
      </footer>
    </div>
  );
};

export default Index;
